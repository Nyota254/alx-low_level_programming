#include "main.h"

/**
 * print_alphabet -prints characters of the alphabet 
 * 
 */

void print_alphabet(void)
{
    char i = 'a';
    for(i = 'a';i <= 'z';i++)
    {
        _putchar(i);
    }
    _putchar('\n');
}