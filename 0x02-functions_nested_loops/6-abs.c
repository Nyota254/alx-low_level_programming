#include "main.h"

/**
 * _abs - returns an absolute value
 * 
 * @n:parameter number to be made absolute
 * 
 * Return: returns an absolute value
 */

int _abs(int n)
{
    if(n >= 0)
        return (n);
    else
        return (-n);
}
